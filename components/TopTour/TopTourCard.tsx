import React from 'react';

interface iTopTour {
  image: any;
  title: string;
  id: number;
  popularPlaces: number;
  locationID: number;
}

const TopTourCard = ({ image, popularPlaces, title, id, locationID }: iTopTour) => (
  <>
  </>
);

export default TopTourCard;
